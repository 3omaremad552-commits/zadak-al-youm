# وثيقة تصميم تطبيق «مسابقة القرآن والحديث»

## اتجاه التصميم

التطبيق مخصص لهواتف Android في الوضع الرأسي بنسبة 9:16، مع اعتماد استخدام بيد واحدة. الواجهة عربية من اليمين إلى اليسار، هادئة ومحترمة للمحتوى الإسلامي، وتستخدم بطاقات واضحة ومساحات لمس كبيرة. لا توجد عناصر تفاعلية صغيرة أو ازدحام بصري.

## الشاشات

| الشاشة | المحتوى والوظيفة |
|---|---|
| تسجيل الدخول | حقل الاسم، حقل كلمة المرور، زر الدخول، ورابط إنشاء حساب. لا نطلب بريدًا إلكترونيًا في النسخة الأولى بناءً على المتطلب الحالي. |
| إنشاء الحساب | اسم المستخدم، كلمة المرور، تأكيد كلمة المرور، والموافقة على قواعد الاستخدام. الاسم الظاهر في لوحة الصدارة يجب ألا يكون رقم هاتف أو بيانات شخصية. |
| الرئيسية | ترحيب باسم المستخدم، مجموع النقاط، بطاقة الأشخاص النشطين حاليًا، اختصارات للأسئلة والتسميع والتسبيح، وملخص ترتيب المستخدم. |
| الأسئلة | اختيار القسم: آيات القرآن، الأحاديث، معلومات إسلامية متنوعة. عرض سؤال واحد في كل مرة، إجابات كبيرة، تغذية راجعة فورية، ونقاط الإجابة الصحيحة. |
| التسميع والاستماع | اختيار السورة أو المقطع، تشغيل تلاوة موثوقة، زر التسجيل، حالة التسجيل، ثم نتيجة مبدئية مع تنبيه واضح أن التقييم الآلي ليس فتوى أو تصحيحًا شرعيًا نهائيًا. |
| التسبيح | دائرة كبيرة في منتصف الشاشة مكتوب داخلها «اضغط»، وعدّاد الجلسة، إجمالي نقاط التسبيح، اختيار الذكر، زر تصفير الجلسة، واهتزاز خفيف عند النقر. |
| لوحة الصدارة | ترتيب المستخدمين حسب النقاط، تبويبات يومية وأسبوعية وكلية، إبراز المستخدم الحالي، وقسم منفصل للأشخاص النشطين الآن. |
| الملف الشخصي | الاسم الظاهر، إجمالي النقاط، إحصاءات الإجابات والتسميع والتسبيح، وتسجيل الخروج. |
| الإعدادات | التحكم بالصوت والاهتزاز، سياسة الخصوصية، شروط الاستخدام، وإدارة الحساب. |

## الهوية البصرية

الألوان الأساسية: أخضر زمردي داكن `#0F5132` للهوية والأزرار الرئيسية، أخضر نعناعي فاتح `#DFF3E7` للخلفيات الهادئة، ذهبي دافئ `#C99A2E` للنقاط والإنجازات، كحلي فحمي `#17212B` للنصوص، وأبيض عاجي `#FCFAF4` للخلفية. حالات النجاح تستخدم `#2E8B57`، والتنبيه `#B7791F`، والخطأ `#B83232`.

تستخدم البطاقات زوايا مستديرة 20px، أزرارًا بارتفاع لا يقل عن 48px، وظلًا خفيفًا فقط. العناوين كبيرة وواضحة، والنصوص الدينية عالية التباين مع تجنب زخرفة النص بما يصعّب القراءة.

## التدفقات الأساسية

### الدخول

يفتح المستخدم التطبيق، يكتب الاسم وكلمة المرور، ثم يضغط «دخول». بعد نجاح التحقق ينتقل إلى الرئيسية. المستخدم الجديد يضغط «إنشاء حساب»، يحدد اسمًا ظاهرًا وكلمة مرور، ثم يعود تلقائيًا إلى الرئيسية.

### الإجابة عن سؤال

من الرئيسية يضغط المستخدم «ابدأ الأسئلة»، يختار القسم، يجيب عن السؤال، يرى صحة الإجابة والنقاط المكتسبة، ثم ينتقل للسؤال التالي أو يعود إلى الرئيسية. تُرسل النقاط إلى الخادم عند توفر الحساب، مع منع احتساب الإجابة مرتين.

### التسبيح

يفتح المستخدم شاشة التسبيح، يختار الذكر إن أراد، ثم يضغط الدائرة. كل ضغطة تزيد عداد الجلسة وتضيف نقطة تسبيح وفق سياسة واضحة، مع تحديث المجموع المحلي والبعيد. زر «تصفير الجلسة» يصفر عداد الجلسة فقط ولا يحذف النقاط السابقة.

### الأشخاص النشطون ولوحة الصدارة

عند فتح الرئيسية أو لوحة الصدارة، يظهر الأشخاص الذين أرسلوا نشاطًا خلال نافذة زمنية قصيرة على أنهم «نشطون الآن». تعرض القائمة الاسم الظاهر والنقاط والترتيب فقط، ولا تعرض كلمة المرور أو أي بيانات حساسة. تُحدّث القائمة دوريًا عند فتح الشاشة أو استئناف التطبيق.

### الاستماع والتسميع

يختار المستخدم مقطعًا، يستمع إلى تلاوة مرخصة أو موثوقة، ثم يبدأ التسجيل بإذن واضح من الميكروفون. يحفظ التسجيل مؤقتًا أثناء الجلسة، ويُعالج فقط إذا كانت ميزة التقييم متاحة. لا يُمنح المستخدم وعدًا بدقة تصحيح التجويد، وتظهر رسالة مراجعة بشرية أو تعليمية عند الحاجة.

## قواعد المنتج

المحتوى القرآني والحديثي يجب أن يمر بمراجعة مصدر موثوق قبل إدخاله. الأسئلة التي تحتمل خلافًا فقهيًا لا تستخدم بصيغة إجابة قطعية في النسخة الأولى. نظام النقاط لا يجعل العبادة منافسة إلزامية، ويعرض التسبيح كأداة متابعة شخصية مع لوحة صدارة اختيارية قابلة للإخفاء.

الإعلانات، عند إضافتها، توضع في مواضع غير مزعجة ولا تظهر فوق نص قرآني أو أثناء التلاوة أو التسجيل. لا تُحتسب النقاط من النقر على الإعلانات، ولا يُطلب من المستخدم النقر عليها.
